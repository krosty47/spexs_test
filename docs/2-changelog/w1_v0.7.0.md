# Changelog - Week 1, 02-04-2026, V. 0.7.0

**Release Date**: Week 1, 02-04-2026 at 23:12
**Version**: 0.7.0 (previously 0.6.0)
**Object**: feat: full-stack audit & hardening — Redis removal, security hardening, type safety, architecture, error handling, tRPC structure, XSS sanitization

## Changes

### Task 1 — Redis Removal
- Removed unused Redis service from `docker-compose.yml` and `redis_data` volume
- Removed `REDIS_URL` and `REDIS_PASSWORD` from `.env.example` and `.env`
- Removed `REDIS_URL` from `env.validation.ts` Zod schema
- Updated `docs/ARCHI.md` to clarify Redis is not currently deployed

### Task 2 — Security Hardening
- Added 1MB `bodyLimit` to FastifyAdapter in `main.ts`
- Configured Helmet with CSP directives (disabled in development)
- Updated CORS to parse comma-separated origins from `CORS_ORIGIN`
- Created `RateLimitMiddleware` as an in-memory tRPC middleware (5 req/60s per IP)
- Applied rate limiting to `auth.login`, `auth.register`, and `auth.refresh`
- Rate limiter includes periodic cleanup of expired entries

### Task 3 — Type Safety Audit
- Confirmed all 21 tRPC procedures have Zod input and output schemas
- Removed inline type annotations from `event-detail.component.tsx` (types inferred from tRPC)

### Task 4 — Separation of Responsibilities
- Extracted `UsersService` from `UsersRouter` (was the only router with direct Prisma access)
- Updated `UsersModule` to register the new service
- Confirmed all other routers properly delegate to services

### Task 5 — Error Handling
- Enhanced `GlobalExceptionFilter` with structured logging via NestJS Logger
- Added stack trace stripping in production for non-tRPC errors
- Confirmed all services throw `TRPCError` with correct codes
- Confirmed frontend ErrorBoundary and global mutation toast handler

### Task 6 — tRPC Structure
- Removed `[key: string]: unknown` index signature from `AppContextType`
- Confirmed protectedProcedure pattern (AuthMiddleware), router merging, and frontend client setup

### Task 7 — Comment XSS Sanitization
- Added `.trim()` and HTML-stripping `.transform()` to `addCommentSchema` content field
- Added `.pipe(z.string().min(1))` to reject HTML-only content after stripping
- Confirmed frontend renders comments safely via React JSX auto-escaping

### Task 8 — Verification
- Created verification checklist at `docs/6-memo/audit-hardening-v0.7.0-checklist.md`
- All 148 backend tests pass (17 new tests added)
- Build, lint, and TypeScript type-check all pass

### New Files
- `apps/backend/src/trpc/rate-limit.middleware.ts` — In-memory rate limiter
- `apps/backend/src/trpc/rate-limit.middleware.spec.ts` — 4 tests
- `apps/backend/src/common/filters/global-exception.filter.spec.ts` — 7 tests
- `apps/backend/src/features/users/users.service.ts` — Extracted service
- `apps/backend/src/features/users/users.service.spec.ts` — 2 tests
- `docs/6-memo/audit-hardening-v0.7.0-checklist.md` — Verification checklist
